apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plutonia.fullname" . }}-test-connection"
  labels:
    {{- include "plutonia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plutonia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
