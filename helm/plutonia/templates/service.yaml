apiVersion: v1
kind: Service
metadata:
  name: {{ include "plutonia.fullname" . }}
  labels:
    {{- include "plutonia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plutonia.selectorLabels" . | nindent 4 }}
